language: java

env:
  global:
    - EnableNuGetPackageRestore=false

services:
  - elasticsearch

script: 
  - sudo apt-get install mono-devel nunit-console -y
  - bundle install 
  - rake install_packages
  - rake test
  - rake ruby:test
